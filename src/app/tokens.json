{
  "colors": {
    "$description": "Color palette tokens",
    "accent": {
      "$type": "color",
      "$value": "#3ffa15",
      "$description": "Accent color for highlights"
    },
    "dark": {
      "$type": "color",
      "$value": "#000",
      "$description": "Dark neutral color for backgrounds"
    },
    "light": {
      "$type": "color",
      "$value": "#f9fafb",
      "$description": "Light neutral color for text on dark backgrounds"
    },
    "primary": {
      "$type": "color",
      "$value": "#1e40af",
      "$description": "Primary brand color"
    },
    "secondary": {
      "$type": "color",
      "$value": "#2563eb",
      "$description": "Secondary brand color"
    }
  },
  "spacing": {
    "$description": "Spacing scale derived from base unit",
    "large": {
      "$type": "dimension",
      "$value": "calc(var(--spacing) * 40)",
      "$description": "Large spacing multiplier; calc expression references base --spacing variable"
    },
    "medium": {
      "$type": "dimension",
      "$value": "calc(var(--spacing) * 12)",
      "$description": "Medium spacing multiplier; calc expression references base --spacing variable"
    },
    "small": {
      "$type": "dimension",
      "$value": "calc(var(--spacing) * 6)",
      "$description": "Small spacing multiplier; calc expression references base --spacing variable"
    }
  }
}
